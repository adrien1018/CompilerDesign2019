int meow () {}

int main ()
{
  int i, j;
  A();
  B(a+b);
  C(1+c);
  d = D();
  e = 1 + 2 * 3;
  if (f == g);
  else if (h + i > 3) j = k;

  for (l = 0; m != 0; n = o + 1);

  while (p > 0) q = r + 1;
  return s * t;
  return E();
  meow(x);
  return meow(x);
}
